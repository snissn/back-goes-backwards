{
  "name": "axial-load-gradient-model",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "lint": "pnpm -r --if-present lint",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build",
    "dev": "pnpm -C apps/web dev",
    "pdf": "bash tools/build-pdf.sh"
  }
}
