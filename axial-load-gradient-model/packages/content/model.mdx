import { drills } from "@algm/data";

# Data-backed drills (placeholder)

This file demonstrates that content imports the shared drill data.

<ul>
  {drills.map((d) => (
    <li key={d.id}>{d.id}: {d.name}</li>
  ))}
</ul>
